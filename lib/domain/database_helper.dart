class DatabaseHelper {

  // 1. ViewModels can only import this class (the can't touch the DataBaseInteractor)
  // 2. This class will be used for app specific use cases, such as querying the database
  // before saving a movie so that we can make the heart icon highlighted or not
}
